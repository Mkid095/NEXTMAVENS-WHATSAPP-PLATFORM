✅ NEXTMAVENS WhatsApp Platform Backend - RUNNING

Server Status: ACTIVE
─────────────────────────────────────────────────────────────

🌐 Server Configuration:
  Port: 9403 (non-default to avoid conflicts)
  Host: 0.0.0.0 (listens on all interfaces)
  Environment: Development (NODE_ENV not set to production)

📡 Endpoints:
  Health Check:  GET    http://localhost:9403/health
  Ping:         GET    http://localhost:9403/ping
  Metrics:      GET    http://localhost:9403/metrics (Prometheus format)
  Webhooks:     POST   http://localhost:9403/api/webhooks/evolution

🔐 Authentication:
  All /api/* routes require valid JWT (except webhooks which use signature verification)
  Public endpoints: /ping, /metrics, /health, /api/webhooks/evolution

📊 Metrics Collection (Prometheus):
  ✅ HTTP request/response metrics
  ✅ BullMQ queue metrics
  ✅ Instance heartbeat metrics
  ✅ Node.js process metrics
  ✅ Redis connection metrics (defined, values 0 if no activity)
  ✅ Prisma database metrics (defined, values 0 if no queries yet)

  Total metric types: 53
  Access: http://localhost:9403/metrics

🔧 Development:
  Process: tsx watch src/server.ts
  Logs: /tmp/server-9403.log
  Build: npm run build (outputs to dist/)
  TypeScript: No compilation errors

⚠️  Warnings:
  - Redis eviction policy: "allkeys-lru" should be "noeviction" for production
    This is a Redis server configuration issue, not a code bug.
    Fix: Set `maxmemory-policy noeviction` in redis.conf

🔌 Features Disabled for Diagnostics:
  - Socket.IO (commented out in server.ts lines 424-432)
    To re-enable: uncomment those lines

📝 Documentation:
  - GRAFANA_DASHBOARD_SETUP.md - Grafana panel specs and Prometheus config
  - METRICS_COMPLETION_SUMMARY.md - Full completion report and checklist

─────────────────────────────────────────────────────────────
To stop the server: pkill -f "tsx watch src/server.ts"
To start: PORT=9403 npm run dev
─────────────────────────────────────────────────────────────
